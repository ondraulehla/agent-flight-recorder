id: hello-world
prompt: |
  Create a file named hello.txt containing exactly the line "Hello from the sandbox!"
  and nothing else. Do not create any other files.
assertions:
  - kind: file_exists
    path: hello.txt
  - kind: command_succeeds
    command: grep -qx "Hello from the sandbox!" hello.txt
timeout_s: 300
